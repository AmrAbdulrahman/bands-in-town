import React, { Component } from 'react';
import LoadingBar from '../LoadingBar';

class Head extends Component {
  render() {
    return (
      <div>
        <LoadingBar />
      </div>
    );
  }
}

export default Head;
